<div class="post">

  <p>
    <% @posts.each do |p| %>
      <h1 id="post"><%= p.title %></hl>
      <h3 class="entry">by Admin on <%= p.created_at.to_date %></h3>
      <%= p.text %>
      <hr />
    <% end %>
  </p>
  
</div>